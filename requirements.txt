# Base image (ghcr.io/gonka-ai/vllm:v0.9.1-blackwell) already includes:
# - torch, triton, transformers, accelerate, numpy, scipy
# - vLLM with Blackwell GPU support and FP8 kernels

# Runpod SDK (for serverless worker)
runpod

# HTTP client for warmup callback
requests>=2.31.0

# Additional dependencies
tiktoken>=0.8.0
fire>=0.5.0
tqdm>=4.66.0
